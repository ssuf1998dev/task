# yaml-language-server: $schema=../../website/static/next-schema.json
version: "3"

plugins:
  hello:
    file: hello.wasm

tasks:
  default:
    cmd: echo {{hello_hello "world"}} > hello_hello_world.txt
