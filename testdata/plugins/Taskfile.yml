# yaml-language-server: $schema=../../website/static/schema.json
version: "3"

plugins:
  hello:
    file: hello.wasm

tasks:
  default:
    cmd: echo {{plugin "hello" "hello" "world"}} > hello_hello_world.txt
